language:
  scala

scala:
  - 2.12.7

script:
  - sbt clean coverage test coverageReport

# Safe list - only do branch build for "master" (keep doing PR builds for
# every pr)
branches:
  only:
    - master