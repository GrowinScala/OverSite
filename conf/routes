# Routes
# This file defines all application routes (Higher priority routes first)



#GET     /users/:usersID/chats
#GET     /users/:usersID/chats/:chatID/emails
#GET     /users/:usersID/chats/:chatID/emails/:emailID

#GET     /users/:userID/shares
#GET     /users/:userID/shares/:shareID

#apagar##################
GET      /index                   api.controllers.EmailsController.index
#########################

#send an email
POST     /users/:userID/email     api.controllers.EmailsController.email(userID: String)

#Replying to an email
#POST    /users/:userID/chats/:chatID

#PATCH   /users/:userID/chats/:chatID


#authentication:
POST    /signin                   api.controllers.UsersController.signin
POST    /login                    api.controllers.UsersController.login
#POST    /logout

