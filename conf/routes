# Routes
# This file defines all application routes (Higher priority routes first)


#inbox
GET     /users/:usersID/chats    api.controllers.ChatController.inbox(usersID: String)
#sent
GET     /users/:usersID/emails/:status api.controllers.EmailsController.showEmails(usersID: String, status: String)
#GET     /users/:usersID/emails/
#GET     /users/:usersID/chats/:chatID/emails/:emailID

POST     /users/:usersID/shares          api.controllers.ChatController.supervised(usersID: String)
#GET     /users/:userID/shares/:shareID

#apagar##################
POST      /index                   api.controllers.EmailsController.index
#########################

#send an email
POST     /users/:userName/email   api.controllers.EmailsController.email(userName: String)

#Replying to an email
#POST    /users/:userID/chats/:chatID

#PATCH   /users/:userID/chats/:chatID


#authentication:
POST    /signin                   api.controllers.UsersController.signin
POST    /login                    api.controllers.UsersController.login
#POST    /logout

